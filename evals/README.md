# Evaluation fixtures

`fixtures.jsonl` contains property-oriented regression cases. It intentionally does not prescribe one exact ideal message.

Each fixture records conversational properties that must remain stable, such as planner action, CTA permission, question permission, length budget and first-outreach name requirements.

This format is intentionally simple so the same cases can later feed model-backed DeepEval or Promptfoo runs.
